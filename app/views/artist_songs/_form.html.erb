<%= form_for(artist_song) do |f| %>
  <% if artist_song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist_song.errors.count, "error") %> prohibited this artist_song from being saved:</h2>

      <ul>
      <% artist_song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :artist_id %>
    <%= f.text_field :artist_id %>
  </div>

  <div class="field">
    <%= f.label :song_id %>
    <%= f.text_field :song_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
